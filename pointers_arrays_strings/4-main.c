#include "main.h"

/**
 * main - check the print_rev function
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac asimov";
	print_rev(str);

	return (0);
}
